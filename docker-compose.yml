version: "3.7"
services:
    nginx:
        build: ./nginx
        restart: always
        ports:
            - "20000:20000"
        volumes:
            - "./www:/www"
            - "./nginx/nginx.conf:/etc/nginx/nginx.conf"
            - "./nginx/conf.d/bitrix.conf:/etc/nginx/conf.d/bitrix.conf"
    php-fpm:
        build: ./php-fpm
        restart: always
        volumes:
            - "./www:/www"
            - "./php-fpm/php.ini:/usr/local/etc/php/php.ini"
    mysql:
        build: ./mysql
        restart: always
        volumes:
            - "./mysql/conf.d/bitrix.conf:/etc/mysql/conf.d/bitrix.conf"
        environment:
            MYSQL_ROOT_PASSWORD: 123456
            MYSQL_DATABASE: "www-data"
            MYSQL_USER: "www-data"
            MYSQL_PASSWORD: "123456"
